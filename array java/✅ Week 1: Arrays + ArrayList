notes java

Arrays In Java

Arrays in Java are like a list of elements of the same type i.e. a list of integers, a list of booleans etc. 

Arrays are used to store a collection of elements of the same data type in a single variable.


Arrays are declared in the following way:

Creating an Array (method 1) - with new keyword
int[] marks = new int[3];
marks[0] = 97;
marks[1] = 98;
marks[2] = 95;

Creating an Array (method 2)
int[] marks = {98, 97, 95};

Taking an array as an input and printing its elements.
import java.util.*;


public class Arrays {
   public static void main(String args[]) {
       Scanner sc = new Scanner(System.in);
       int size = sc.nextInt();
       int numbers[] = new int[size];


       for(int i=0; i<size; i++) {
           numbers[i] = sc.nextInt();
       }


       //print the numbers in array
       for(int i=0; i<arr.length; i++) {
           System.out.print(numbers[i]+" ");
       }
   }
}




✅ Week 1: Arrays + ArrayList
🎥 Already Done: Apna College videos

🔨 Practice (Start Easy → Medium):

🔹 Easy:

Find max & min in array
Sum of all elements
Linear Search
Check if array is sorted
Reverse the array
Left/right rotate array
🔹 Level Up (Medium):
7. Second largest element
8. Move all zeros to end
9. Remove duplicates from sorted array
10. Kadane’s Algorithm (Max Subarray Sum)